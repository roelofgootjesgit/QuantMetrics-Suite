# EXP-003 — Plan

Pre-geregistreerd venster en vijf instrumenten; uitvoering via `quantbuild/configs/experiments/exp003_overlap_breakout/*.yaml` en `quantmetrics_os/scripts/run_exp003_matrix.py`.

Afgerond met REJECT — zie `decision.md`.
